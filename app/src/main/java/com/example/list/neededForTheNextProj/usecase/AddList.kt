package com.example.list.neededForTheNextProj.usecase

import com.example.list.data.InvalidListException
import com.example.list.data.ListEntity
/*
class AddList(
    private val repository: ListRepository
) {
    suspend operator fun invoke(list: ListEntity){
        if(list.listName?.isBlank() == true){
            throw InvalidListException("List can not be null")

        }
        repository.insertList(list)
    }
}

 */